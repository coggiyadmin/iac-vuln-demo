# TP — container adds SYS_ADMIN capability (misconfig)
apiVersion: v1
kind: Pod
metadata:
  name: mounter
spec:
  containers:
    - name: app
      image: alpine:3.19
      securityContext:
        capabilities:
          add: ["SYS_ADMIN"]
