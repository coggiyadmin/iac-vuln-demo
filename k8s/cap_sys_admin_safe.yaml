# SAFE — drop all capabilities
apiVersion: v1
kind: Pod
metadata:
  name: mounter
spec:
  containers:
    - name: app
      image: alpine:3.19
      securityContext:
        capabilities:
          drop: ["ALL"]
